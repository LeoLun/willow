import { Injectable } from "poetry";

@Injectable()
export class DialogService {

  openRenameDialog({ targetPath, recommendations }: { targetPath: string; recommendations: string[] }) {
    // 打开对话框
  }

}