## 状态说明

- 当前 change 处于规格定义完成、待进入实现规划状态。
- 本次任务聚焦聊天输入区、技能发现与发送协议，不扩展为完整技能管理中心。

## 1. 输入区改造

- [x] 1.1 用 Tiptap 替换当前 `Sender` 中的普通文本输入控件。
- [ ] 1.2 保持发送、停止、模型切换、联网开关与 token 用量展示行为不回退。
- [x] 1.3 明确编辑器纯文本序列化、空消息校验和发送后清空规则。

## 2. 技能发现与选择

- [x] 2.1 新增主进程技能发现接口，返回全局技能与当前工作空间技能摘要。
- [x] 2.2 在输入区增加技能选择入口、slash 搜索、分组展示和已选标签展示。
- [x] 2.3 处理无工作空间、无技能、技能失效等空态和降级提示。
- [x] 2.4 在技能结果项末尾展示来源标签，并明确区分 `全局` 与 `工作空间`。

## 3. 发送链路

- [x] 3.1 扩展 shared API 的消息发送结构，允许携带本轮选中技能。
- [x] 3.2 在主进程发送前校验选中技能，并将其组装为受控 prompt 前缀。
- [x] 3.3 确保本轮技能选择不会污染下一轮消息状态。

## 4. 验证

- [ ] 4.1 手动验证无技能时普通聊天发送仍可用。
- [ ] 4.2 手动验证仅全局技能、仅工作空间技能、两者同时存在时的展示与选择行为。
- [ ] 4.3 手动验证 slash 查询可以实时筛选技能，并在确认后转成顶部技能胶囊。
- [ ] 4.4 手动验证每个技能结果项末尾都正确显示 `全局` 或 `工作空间` 来源标识。
- [ ] 4.5 手动验证已选技能随发送清空，且 stop / route 切换不会造成编辑器异常。
- [ ] 4.6 对照 [`DESIGN.md`](/Users/liujinglun/code/willow/DESIGN.md) 检查输入区密度、弹层层级与主操作稳定性。
- [ ] 4.7 对照用户提供的参考图，检查输入区是否形成“顶部技能胶囊 + 中部编辑区 + 底部控制条 + 右侧发送按钮”的整体骨架。

## 5. 交接

- [ ] 5.1 进入 `workflow-worktree` 准备隔离实现环境。
- [ ] 5.2 进入 `workflow-plan`，按 renderer、shared API、main、验证四个切片拆分实施顺序。
