# context-compression 任务

## 1. 上下文预算与切分

- [x] 1.1 梳理 `AgentMessage` 结构，定义可测试的 token 估算函数。
- [x] 1.2 实现上下文预算计算：`usableContext`、触发阈值、目标预算、输出余量。
- [x] 1.3 实现按用户消息边界切分会话轮次，并支持保留最近 3-5 轮完整消息。
- [x] 1.4 覆盖预算和切分的单元测试或等价验证用例。

## 2. 摘要状态持久化

- [x] 2.1 设计并添加上下文摘要表或等价持久化结构。
- [x] 2.2 为摘要状态添加 DAO：按会话读取、创建/更新、删除。
- [x] 2.3 删除会话时同步清理摘要状态。
- [x] 2.4 验证原始 `session_messages` 查询和聊天历史展示不受影响。

## 3. 摘要生成

- [x] 3.1 在主进程实现隔离的摘要 Agent 创建逻辑，复用当前模型配置但禁用工具。
- [x] 3.2 编写稳定的中文摘要提示词，覆盖摘要、决策、已完成操作、待跟进、索引。
- [x] 3.3 支持增量摘要：合并旧摘要与新压缩消息，更新压缩游标。
- [x] 3.4 实现摘要超时、失败捕获和保守降级路径。

## 4. AgentService 集成

- [x] 4.1 在 `getDefaultAgent()` 构建主 Agent 前执行上下文预算检查。
- [x] 4.2 未触发压缩时保持现有完整历史注入行为。
- [x] 4.3 触发压缩时将摘要注入 `CoreAgent` 系统上下文，并只对主 Agent 注入最近完整窗口。
- [x] 4.4 确保 DeepSeek reasoning 历史归一化仍应用于保留的最近窗口。
- [x] 4.5 确保摘要生成不写入普通会话消息、不触发标题生成、不触发工具审批。

## 5. Renderer 反馈

- [x] 5.1 定义主进程到 renderer 的上下文压缩通知事件与共享类型。
- [x] 5.2 在聊天 renderer 监听当前会话的压缩事件。
- [x] 5.3 使用现有 `vue-sonner` / `@willow/shadcn` 反馈“上下文已自动压缩”。
- [x] 5.4 验证非当前会话事件不产生误提示，降级事件使用可理解文案。

## 6. 验证与收尾

- [x] 6.1 补充或运行相关单元测试 / 手动验证脚本。
- [x] 6.2 运行 `pnpm lint`。
- [x] 6.3 运行 `pnpm build`。
- [x] 6.4 更新必要的上下文说明文档，例如 `packages/core/src/context/README.md`。
