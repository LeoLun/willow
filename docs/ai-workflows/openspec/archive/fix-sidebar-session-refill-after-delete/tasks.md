## 1. Store 数据同步

- [x] 1.1 梳理 `useSessionStore` 中 `deleteSession`、`cleanSession`、`fetchSessionList` 的职责，避免删除路径重复请求或只做本地移除。
- [x] 1.2 增加或调整单工作空间刷新能力，使删除成功后能重新获取该工作空间最新 5 条会话。
- [x] 1.3 确保刷新结果同时更新 `sessionMap[workspaceId]` 和 `sessionTotals[workspaceId]`。

## 2. 删除交互串联

- [x] 2.1 调整 `NavMain.vue` 与 `DeleteSession.vue` 的删除成功回调，使侧边栏删除后触发单工作空间刷新。
- [x] 2.2 确认删除失败时不会提前本地移除会话。
- [x] 2.3 处理删除当前路由会话后的受控导航或空态，避免继续高亮或访问已删除会话。

## 3. 验证

- [x] 3.1 手动验证工作空间总会话数大于 5 时，删除一条侧边栏可见会话后自动补回 5 条。
- [x] 3.2 手动验证删除后总数等于或小于 5 时，“查看更多...”隐藏且列表数量正确。
- [x] 3.3 手动验证删除非当前工作空间会话不会刷新其他工作空间列表。
- [x] 3.4 手动验证删除当前路由会话不会造成空白崩溃或保留已删除会话高亮。
- [x] 3.5 运行受影响范围的 lint / build 校验；若因既有问题无法通过，在结果中明确记录。

## 4. 历史会话页同步

- [x] 4.1 调整 `WorkspaceHistory.vue` 的数据加载生命周期，使 `workspaceId` 路由参数变化时重置 `sessions`、`total`、`page` 并重新加载目标工作空间第一页。
- [x] 4.2 确保从侧边栏删除当前历史页所属工作空间的会话后，历史页刷新或同步移除已删除会话。
- [x] 4.3 确保从侧边栏删除其他工作空间会话时，不刷新或替换当前历史页数据。
- [x] 4.4 手动验证在历史页连续点击不同工作空间的“查看更多...”时，标题、列表、空态和“已加载全部”状态都来自当前路由工作空间。
