# 任务清单

- [ ] 重写 `WorkspaceDelegateToolRenderer.vue`：移除 `ToolCallCard` 依赖，改为固定行内布局
- [ ] 实现 hover 态：整体 `hover:bg-muted/40`，右侧出现 `ChevronRight` 箭头
- [ ] 箭头点击触发 `onNavigateToSession(childSessionId)` 导航
- [ ] 验证：lint 通过、build 通过、dev 运行正常
