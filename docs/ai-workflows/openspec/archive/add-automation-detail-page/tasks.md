## 1. 路由与页面骨架

- [x] 1.1 在不影响现有 `/auto` 列表路由的前提下，新增 `/auto/:automationId` 自动化详情路由。
- [x] 1.2 定义详情页基础结构，包括面包屑或头部、主提示词编辑区和次级配置编辑区域。

## 2. 数据加载与状态处理

- [x] 2.1 复用或扩展自动化 store，使其支持按 id 获取单条自动化详情，并初始化页面编辑状态。
- [x] 2.2 扩展自动化共享类型、详情查询和更新链路，使其支持更新 `workspaceId` 并持久化可选 `modelId`。
- [x] 2.3 定义加载中、未找到、元信息缺失、运行历史缺失、保存中和保存失败时的受控状态。

## 3. 列表导航行为

- [x] 3.1 让自动化列表项可进入对应详情路由。
- [x] 3.2 确保编辑、复制、删除等行内操作不会触发列表项跳转。

## 4. 详情页编辑与信息呈现

- [x] 4.1 按照 `DESIGN.md` 对齐的分区方式展示并编辑标题、完整提示词、状态、工作空间、重复规则、模型和最近执行摘要。
- [x] 4.2 在详情页中根据当前选中模型或默认模型展示推理能力摘要，并处理模型已删除或默认模型缺失时的占位状态。
- [x] 4.3 在详情页动作区提供保存、取消或返回、删除等稳定动作，并保持危险操作在视觉层级上弱于常规操作。
- [x] 4.4 确保保存成功后详情与列表摘要保持一致，保存失败时保留用户未提交输入。

## 5. 自动化执行配置与运行时对齐

- [x] 5.1 在自动化执行链路中使用自动化保存的 `modelId`；当未设置时回退到默认模型。
- [x] 5.2 验证更新工作空间后，后续自动化运行会在新的工作空间中创建会话。

## 6. 验证

- [x] 6.1 手动验证从列表进入详情、从详情返回列表，以及未找到状态的处理。
- [x] 6.2 手动验证标题、提示词、状态、工作空间、重复、模型等字段的修改与保存反馈。
- [x] 6.3 手动验证不同模型切换后推理摘要是否正确变化，以及缺失模型时的占位反馈。
- [x] 6.4 对照 `DESIGN.md` 检查详情页的头部层级、间距密度、编辑控件分组和操作优先级。
