## 状态说明

- 当前 change 已完成最终实现校对。
- 规格要求的右栏折叠、`workspace` / `session` 侧栏内容、`TODO` 迁移、工作空间文件树与 `soul.md` 保存入口均已落地。
- 手动验收与最终命令校验完成后，本次 `workflow-close` 可执行归档。

## 1. 聊天页壳层与折叠能力

- [x] 1.1 在 `Chat.vue` 所在聊天页壳层中加入统一右侧栏容器，并提供右上角折叠 / 展开入口。
- [x] 1.2 调整聊天页主内容区、底部输入区与右侧栏的布局关系，确保展开 / 折叠时主内容宽度变化稳定。
- [x] 1.3 验证右栏切换不会影响消息滚动、流式消息展示和输入框内容。

## 2. Workspace 右侧栏

- [x] 2.1 为 `workspace` 上下文定义工作空间设置面板结构，对齐设计参考中的路径、`soul.md` 与保存区分组。
- [x] 2.2 复用或补充工作空间数据读取 / 保存链路，使右栏展示的设置具备真实数据来源。
- [x] 2.3 对齐 `DESIGN.md` 检查设置面板的分组标题、说明文案、字段密度和主次操作层级。

## 3. Session 右侧栏

- [x] 3.1 为 `session` 上下文实现包含 `概要` 和 `文件` 的右侧栏结构。
- [x] 3.2 将当前输入区上方的 `TODO` 展示迁移到 `概要` 标签的会话信息下方。
- [x] 3.3 为概要区补齐会话基础信息的可用数据映射，例如标题、时间信息和消息数量。

## 4. 工作空间文件列表

- [x] 4.1 新增或补充当前工作空间文件树读取能力，明确数据来源为 `workspace.path`。
- [x] 4.2 在 `文件` 标签中实现目录展开 / 收起和基础文件类型展示。
- [x] 4.3 处理工作空间不存在、不可读、为空目录等受控状态。

## 5. 验证

- [x] 5.1 手动验证 `workspace` 和 `session` 两种上下文下右栏内容切换正确。
- [x] 5.2 手动验证右上角按钮在展开 / 折叠两种状态下都可用且位置稳定。
- [x] 5.3 手动验证 `TODO` 迁移后输入区视觉恢复干净，概要区展示逻辑正确。
- [x] 5.4 手动验证工作空间文件树在正常、空目录和读取失败情况下的表现。
- [x] 5.5 对照 [`DESIGN.md`](/Users/liujinglun/code/willow/DESIGN.md) 检查整体布局层级、信息密度和交互克制度。
