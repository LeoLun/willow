# 任务清单

## 任务

- [x] **T1**：修复 `scrollToBottom()` 函数，增加 `shouldStickToBottom` 守卫
- [x] **T2**：调整 template 结构，为滚动区域添加 `relative` 定位容器
- [x] **T3**：添加"返回底部"浮动按钮（`Button` + `ArrowDown` 图标 + `Transition`）
- [x] **T4**：实现 `handleScrollToBottom` 点击处理函数
- [x] **T5**：添加过渡动画 CSS
- [x] **T6**：验证：流式输出时向上滚动不被拉回、按钮出现、点击返回底部后恢复自动跟随
- [x] **T7**：验证：切换 session 后状态重置、`pnpm lint` 和 `pnpm build` 通过
