# split-settings-page Tasks

## 1. 路由与设置壳层

- [x] 1.1 将设置路由调整为壳层 + 子路由结构，确保 `/setting` 默认进入外观页。
- [x] 1.2 在设置壳层中实现左侧设置导航，包含“返回应用”“外观”“配置”。
- [x] 1.3 为当前设置分区提供稳定选中态，并确保导航点击通过路由切换。
- [x] 1.4 实现“返回应用”行为，无来源时回到 `/`。

## 2. 外观设置页

- [x] 2.1 从当前设置页拆出外观设置内容，形成独立外观子页面。
- [x] 2.2 复用 `useDarkMode()` 与现有 `ThemeMode`，保持主题切换行为不变。
- [x] 2.3 对照 `Willow / Settings / Appearance` 调整标题、说明、分组与 ToggleGroup 密度。
- [x] 2.4 确认进入外观页不会触发模型列表或 Tavily Key 列表加载。

## 3. 配置设置页

- [x] 3.1 从当前设置页拆出模型配置内容，形成配置子页面的模型分组。
- [x] 3.2 从当前设置页拆出 Tavily Key 管理内容，形成配置子页面的网络搜索分组。
- [x] 3.3 保留添加、编辑、删除、设为默认、Key 脱敏、用量进度等现有交互。
- [x] 3.4 对照 `Willow / Settings` 调整配置页标题、分组、列表项、主操作与空状态密度。

## 4. 入口与导航对齐

- [x] 4.1 调整主侧边栏“设置”入口，使其进入新的设置工作区默认页。
- [x] 4.2 调整发送器“前往设置”入口，使模型缺失相关路径进入“配置”页。
- [x] 4.3 检查其他 `router.push("/setting")` 或等价入口，按语义进入外观页或配置页。

## 5. 设计与验证

- [x] 5.1 对照 [`DESIGN.md`](/Users/liujinglun/code/willow/DESIGN.md) 和 `work.pen` 检查设置页布局、分组、按钮层级、图标和密度，尤其确认没有额外主应用侧边栏。
- [x] 5.2 手动验证 `/setting`、`/setting/appearance`、`/setting/configuration` 的刷新与直接访问行为。
- [x] 5.3 手动验证主题切换、模型管理、Tavily Key 管理仍可正常工作。
- [x] 5.4 运行相关检查，至少包含 `pnpm lint`；若改动影响构建边界，再运行 `pnpm build`。

## 6. 设置页独立壳层纠偏

- [x] 6.1 调整顶层应用壳层或路由布局，使 `/setting*` 路由渲染时不显示主应用 `LeftSidebar` 和主应用侧边栏触发按钮。
- [x] 6.2 确认设置页画面只有“设置侧边栏 + 设置内容区”两栏，不出现三栏结构。
- [x] 6.3 确认主应用侧边栏中的“设置”入口仍可进入设置页，且设置页“返回应用”可回到来源或默认主页。
- [x] 6.4 对照 `work.pen` 和用户截图复核设置页左侧栏位置、宽度、导航项和内容区关系。
