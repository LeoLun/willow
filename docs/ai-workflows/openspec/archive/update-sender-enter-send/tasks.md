## 状态说明

- 当前 change 处于规格定义完成、待进入实现规划状态。
- 本次任务只调整聊天发送器的键盘交互语义，不扩展为新一轮输入区重构。

## 1. 发送器键盘语义

- [x] 1.1 梳理 `Sender` 与 `Editor` 之间现有键盘事件流，确认普通输入态与面板态的拦截边界。
- [x] 1.2 在普通输入态下实现 `Enter` 触发发送、`Shift+Enter` 保留换行。
- [x] 1.3 保证发送快捷键仍复用现有 `handleSend()` 校验，不绕过空消息和模型检查。

## 2. 面板优先级保护

- [x] 2.1 保持 slash 技能面板打开时的 `Enter` 高亮确认行为不回退。
- [x] 2.2 保持 `@` 文件面板打开时的 `Enter` 高亮确认行为不回退。
- [x] 2.3 保持 `ArrowUp`、`ArrowDown`、`Escape` 的现有面板语义不变。

## 3. 验证

- [x] 3.1 手动验证普通非空消息按 `Enter` 可发送。
- [x] 3.2 手动验证 `Shift+Enter` 可插入多行换行且不会发送。
- [x] 3.3 手动验证技能面板打开时按 `Enter` 只确认技能，不发送消息。
- [x] 3.4 手动验证文件面板打开时按 `Enter` 只确认文件，不发送消息。
- [x] 3.5 手动验证空白草稿按 `Enter` 不发送。
- [x] 3.6 手动验证点击发送按钮与按 `Enter` 的发送结果一致。

## 4. 交接

- [x] 4.1 进入 `workflow-worktree` 准备隔离实现环境。
- [x] 4.2 进入 `workflow-plan`，把键盘事件改造、回归验证和风险检查拆成执行步骤。
