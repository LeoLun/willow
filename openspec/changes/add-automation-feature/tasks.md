## 1. 数据模型与 IPC 契约

- [x] 1.1 新增自动化数据库 schema 与迁移，持久化名称、工作空间、提示词、触发器类型/配置、执行模式、启用状态和最近执行摘要字段
- [x] 1.2 实现自动化 DAO / service / controller 基础读写能力，支持创建、更新、查询、删除和启停自动化记录
- [x] 1.3 在 `shared` 与 `preload` 中补齐自动化 IPC 常量、类型定义和桥接接口，打通 renderer 可调用的自动化管理 API

## 2. 调度与执行链路

- [x] 2.1 实现可扩展触发器模型，并基于 `node-cron` 与标准 cron 表达式完成定时配置解析、校验、预设模式到 cron 的转换以及调度的注册、刷新和取消
- [x] 2.2 实现 `AutomationRunner` 复用现有 `SessionService` 执行自动化，支持“新建会话”与“复用固定会话”两种模式
- [x] 2.3 在自动化执行完成后回写最近计划时间、实际执行时间、成功/失败状态和错误摘要
- [x] 2.4 在应用初始化流程接入自动化调度恢复，并实现重启后仅补最近一次遗漏计划时间的逻辑

## 3. 自动化页面与表单交互

- [x] 3.1 改造 `/auto` 页面，补齐标题区、右上角“添加自动化”入口和无数据时的空态引导
- [x] 3.2 实现自动化列表展示，呈现名称、工作空间、触发方式、执行模式、启用状态和最近执行摘要
- [x] 3.3 使用现有 shadcn-vue 组件实现自动化创建/编辑表单，支持工作空间选择、提示词输入、每天/每小时/每周/自定义四种定时模式、执行模式切换与启用状态设置
- [x] 3.4 实现预设模式与 cron 表达式之间的双向映射：创建时正确生成 cron，编辑时可识别预设并回填，否则回退为“自定义”
- [x] 3.5 让创建表单默认选中“创建新会话”，并支持用户切换到“复用固定会话”后正确保存
- [ ] 3.6 支持自动化表单接受 AI 草稿默认值，并在从聊天预览卡片进入时正确预填名称、工作空间、提示词、调度方式和执行模式

## 4. AI 工具与聊天预览交互

- [ ] 4.1 在统一工具体系中新增自动化管理工具，覆盖列表、详情、创建草稿、更新和删除能力，并与现有自动化 service / DAO 复用同一业务逻辑
- [ ] 4.2 为自动化创建草稿定义稳定的工具结果结构，并在聊天工具消息渲染层把它展示为自动化预览卡片
- [ ] 4.3 实现“点击预览卡片创建 -> 打开自动化表单 -> 使用预填值正式创建”的交互闭环
- [ ] 4.4 让自动化更新、删除工具接入现有审批链路，确保 AI 写操作不会绕过用户确认

## 5. 联调与验证

- [ ] 5.1 联调自动化 CRUD、启停切换与页面刷新，确认调度注册状态与自动化记录保持一致
- [ ] 5.2 手动验证“每天”“每小时”“每周”“自定义”四种模式的保存、回填与 cron 转换结果，以及标准 cron 表达式校验
- [ ] 5.3 手动验证定时触发、新建会话模式、复用固定会话模式和最近一次执行摘要展示
- [ ] 5.4 手动验证 AI 创建草稿、预览卡片展示、点击创建后的表单预填，以及 AI 更新/删除的审批行为
- [ ] 5.5 手动验证应用重启后的遗漏补偿逻辑，确认多次错过时只补最近一次计划执行
